services:
  postgres:
    image: postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
  

  redis:
    image: redis
    ports:
      - 6379:6379


volumes:
  postgres_data: